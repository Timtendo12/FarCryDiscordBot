const Command = require("../Structures/command.js");
const config = require("../data/config.json");
const pointJSON = require("../data/points.json")

module.exports = new Command({
    name: "points",
    description: "testing points with writing and reading from JSON =",

    async run(message, args, client) {

        const users = guild.getUsers()


    }});
